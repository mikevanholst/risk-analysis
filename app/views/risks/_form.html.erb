<%= form_for(risk) do |f| %>
  <% if risk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(risk.errors.count, "error") %> prohibited this risk from being saved:</h2>

      <ul>
      <% risk.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :label %>
    <%= f.text_field :label %>
  </div>

  <div class="field">


      <br/>
    <%= f.label :probability%>
    <%= f.select :probability, options_for_select(1..10) %>
  </div>

  <div class="field">
    <%= f.label :impact %>
    <%= f.select :impact, options_for_select(1..10)  %>
  </div>

  <div class="field">
    <%= f.label :controls %>
    <%= f.select :controls, options_for_select(1..5)  %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
